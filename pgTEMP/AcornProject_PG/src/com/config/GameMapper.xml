<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="GameMapper">
	
	<select id="newGameListSelect" resultType="GameDTO">
		SELECT *
		FROM game
		ORDER BY gameReleaseDate DESC
	</select>
	
	<select id="recommendGameListSelect" resultType="GameDTO">
		SELECT game.*
		FROM game, rate
		WHERE game.gameNo = rate.gameNo
		ORDER BY rate.gameScore DESC, game.gameReleasedDate DESC
	</select>
	
	<select id="tagGameListSelect" parameterType="java.util.list" resultType="GameDTO">
		SELECT game.*
		FROM game, rate
		WHERE game.gameNo = rate.gameNo
		AND REGEXP_LIKE
		<foreach collection="array" item="arr" open="(game.gameGenre, " close=")" separator=",">
			#{arr}
		</foreach> 
		ORDER BY rate.gameScore DESC, game.gameReleasedDate DESC;
		
	</select>
	
</mapper>